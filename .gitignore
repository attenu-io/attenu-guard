__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/
.hypothesis/
/tmp/
.DS_Store
*.zip
*.tgz
.venv/
venv/
.idea/
.vscode/
.venv-*/
attenu-omnigent-bundle.json
attenu-commerce-bundle.json

# AstrBot writes its runtime data dir into the CWD on import (tests/integrations/test_astrbot.py).
/data/

# Local working directory: throwaway venvs for probing a pinned framework version.
# Untracked AND unignored, it was swept into the sdist and broke `python -m build`
# (a venv symlinks an absolute python, which tarfile refuses).
/scratchpad/
